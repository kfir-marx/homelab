apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rabbitmq
resources:
  - ../../base
  - namespace.yaml
  - statefulset.yaml
  - network-policy.yaml
  - monitoring.yaml
patches:
  - path: config-patch.yaml
