apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: external-ai
resources:
  - ../../base
  - namespace.yaml
  - storage.yaml
  - network-policy.yaml
  - monitoring.yaml
components:
  - ../../components/postgres
patches:
  - path: postgres-pvc-patch.yaml
