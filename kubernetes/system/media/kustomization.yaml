apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - config.yaml
  - storage.yaml
  - jellyfin.yaml
  - servarr.yaml
  - bazarr.yaml
  - qbittorrent.yaml
  - seerr.yaml
  - maintainerr.yaml
  - maintainerr-storage-cleanup.yaml
  - backup.yaml
  - httproutes.yaml
  - network-policy.yaml

configMapGenerator:
  - name: maintainerr-storage-cleanup
    files:
      - cleanup.py=maintainerr_storage_cleanup.py
    options:
      disableNameSuffixHash: true
  - name: qbittorrent-settings
    files:
      - enable-sequential.sh=qbittorrent-enable-sequential.sh
      - reconcile.sh=qbittorrent-settings.sh
    options:
      disableNameSuffixHash: true
