apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
  namespace: immich
spec:
  parentRefs:
    - name: private
      namespace: argocd
      sectionName: private-http
  hostnames:
    - immich.home.547600.xyz
  rules:
    - backendRefs:
        - name: immich-server
          port: 2283

