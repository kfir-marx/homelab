# The deterministic switcher identity, inactive retained PostgreSQL recovery
# volume, and private GPU-backed local LLM. The Telegram Codex client itself
# runs on ubuntu-workstation; the LLM exposes only a ClusterIP API.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: homelab-assistant
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/kfir-marx/homelab.git
    targetRevision: main
    path: kubernetes/system/homelab-assistant
  destination:
    server: https://kubernetes.default.svc
    namespace: homelab-assistant
  # Intentionally manual for the one-time poller/database cutover. First sync
  # without prune to issue the switcher credential, then prune only after the
  # workstation gateway is running and rollback data has been verified.
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
